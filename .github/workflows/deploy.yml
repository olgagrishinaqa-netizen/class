name: Class Site CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Lint with flake8
      run: |
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

  build-and-deploy:
    needs: lint-and-test
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    # Стабильная установка утилиты yc в дефолтную папку пользователя runner
    - name: Install Yandex Cloud CLI
      run: |
        curl -sSL https://yandexcloud.net | bash
        # Добавляем путь к yc в системный PATH гитхаба для следующих шагов
        echo "/home/runner/yandex-cloud/bin" >> $GITHUB_PATH

    # Авторизуемся, используя явный полный путь к установленной утилите yc
    - name: Authenticate in Yandex Cloud
      id: yclogin
      run: |
        echo '${{ secrets.YC_SA_JSON_CREDENTIALS }}' > sa_key.json
        /home/runner/yandex-cloud/bin/yc config profile create sa-profile || true
        /home/runner/yandex-cloud/bin/yc config set service-account-key sa_key.json
        /home/runner/yandex-cloud/bin/yc config set folder-id b1g7f31a1r5b3kl9b3f7
        
        # Генерируем IAM-токен
        IAM_TOKEN=$(/home/runner/yandex-cloud/bin/yc iam create-token)
        echo "::add-mask::$IAM_TOKEN"
        echo "iam_token=$IAM_TOKEN" >> $GITHUB_OUTPUT
        rm sa_key.json

    - name: Configure Docker for Yandex Registry
      run: |
        /home/runner/yandex-cloud/bin/yc container registry configure-docker --token=${{ steps.yclogin.outputs.iam_token }}

    - name: Build and Push Docker Image
      run: |
        docker build -t cr.yandex/crp7cl7iq7aba09i8kd0/class-site:v1 .
        docker push cr.yandex/crp7cl7iq7aba09i8kd0/class-site:v1

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v2

    - name: Terraform Init and Apply
      run: |
        terraform init
        terraform apply -var="yc_token=${{ steps.yclogin.outputs.iam_token }}" -replace="yandex_serverless_container.class_container" -auto-approve
      env:
        YC_TOKEN: ${{ steps.yclogin.outputs.iam_token }}
